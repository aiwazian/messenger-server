export class PushNotificationPayload {
	title: string
	body: string
	chatId: string

	/**
	 * Когда сообщение было отправлено, в миллисекундах.
	 *
	 * Клиент ставит это время в setWhen() уведомления. Без него в шторке
	 * стоял бы момент доставки: сообщение, пролежавшее час в очереди FCM или
	 * дождавшееся выхода устройства из спящего режима, выглядело бы только что
	 * написанным.
	 */
	sendTime: string
}
